const styles = () => ({
    col: {
        display: "inline"
    },
    img: {
        marginBottom: "20px",
        transition: "transform 0.3s",
        '&:hover': {
            cursor: "pointer",
            transform: "scale(1.1)"
        }
    }
});

export default styles;